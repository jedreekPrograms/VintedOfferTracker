# History status labels

`historyStatusLabel.ts` centralizes human-readable labels for terminal and runtime listing statuses, including the full-audit `SKIPPED_ALREADY_NEGOTIATED` state. History currently exposes only purchased/user-skipped decisions; the helper is also reusable by broader listing/status views as they surface additional terminal states.
